ClassicEditor.create(document.querySelector(".editor"), {
    // Editor configuration.
}).then((editor) => {
    window.editor = editor;
});
ClassicEditor.create(document.querySelector(".editorId1"), {
    // Editor configuration.
}).then((editor) => {
    window.editor = editor;
});
// .catch(handleSampleError);

// ClassicEditor.create(document.querySelector(".editor1"), {
//     // Editor configuration.
// })
//     .then((editor) => {
//         window.editor = editor;
//         editor.setData(editor.sourceElement.value);
/////         !readonly &&
//             editor.model.document.on("change:data", (evt, data) => {
//                 editor.sourceElement.value = editor.getData();
//             });
//     })
//     .catch(handleSampleError);
// name="group-a[0][description]"
// ClassicEditor.create(document.querySelector(".item_description_editor"), {
//     // Editor configuration.
// })
//     .then((editor) => {
//         window.editor = editor;
//         editor.setData(editor.sourceElement.value);

//         // Assuming `readonly` is a variable defined elsewhere
//         if (!readonly) {
//             editor.model.document.on("change:data", (evt, data) => {
//                 editor.sourceElement.value = editor.getData();
//             });
//         }
//     })
//     .catch(handleSampleError);

// function handleSampleError(error) {
//     const issueUrl = "https://github.com/ckeditor/ckeditor5/issues";

//     const message = [
//         "Oops, something went wrong!",
//         `Please, report the following error on ${issueUrl} with the build id "stqp062jc7n1-408z8o15t8dv" and the error stack trace:`,
//     ].join("\n");

//     console.error(message);
//     console.error(error);
// }
